public class matrix {

  public static void matrix(string[]arg)
  {
    scanner scanner = new scanner(system in);
    system outprintln ("enter the num"'a');
    double a = scanner.next double();
    system outprintln("enter the num"'b');
    double b = scanner.next double();
    system outprintln("enter the num"'c')
    double c = scanner.next double();
    double d = scanner.next double();
    double determinant = cal determinant (a,b,c,d);
    systemout.println ("the det is:"determinant);
    scanner.(lose());
  }
  private static double(double a, double b, double c, double d);
  return(a*d)-(b*c);
}
}